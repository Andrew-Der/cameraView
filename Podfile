# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'cameraView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cameraView
  pod 'SwiftyCam'
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Storage'

  target 'cameraViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cameraViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
